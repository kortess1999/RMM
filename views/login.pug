extends layout
block content
    div.row
        div.col
            form(action="/login/auth" method="POST")
                fieldset
                    legend Ауентификация
                        p
                            label(for="login") Логин
                            input(type="text" id="login")
                        p
                            label(for="pass") Пароль
                            input(type="text" id="pass")
                p
                    input(type="submit" value="Отправить")
